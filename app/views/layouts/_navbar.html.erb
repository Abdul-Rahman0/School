<nav class="navbar navbar-expand-lg navbar-light bg-body-tertiary">
  <div class="container-fluid">
    <%= link_to "Home", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if school_login_signed_in? %>
          <li class="nav-item">
            <%= link_to current_school_login.email, edit_school_login_registration_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign out", destroy_school_login_session_path, 'data-turbo-method': :delete, class: "nav-link", data: { confirm: "Are you sure you want to log out?" } %>
          </li>
          <li class="nav-item">
            <!-- to show the current role and portal  -->
            <%= link_to "portal", portal_info_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <!-- Button to show the current role -->
            <button class="btn btn-outline-primary nav-link" type="button" disabled>
              Current Role: <%= current_school_login.role.titleize %>
            </button>
          </li>
        <% else %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Login/Signup
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Log in", new_school_login_session_path, class: "dropdown-item" %></li>
              <li><hr class="dropdown-divider"></li>
              <li><%= link_to "Sign up", new_school_login_registration_path, class: "dropdown-item" %></li>
            </ul>
          </li>
        <% end %>
      </ul>
      <form class="d-flex" role="search">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>
